// import { describe, test, expect } from 'vitest';
// import apiClient from './client';

// describe('APIクライアントが正常に動作する', async () => {
//   test('コンバージョンを保存する', async () => {
//     const response = await apiClient('saveConversion', 'POST', {
//       user_id: '123',
//       conversion_key: 'abc',
//     });
//     expect(response).toBeDefined();
//   });
// });

// 仮のテスト
import { test, expect } from 'vitest'
test('テスト', () => {
	expect(1 + 1).toBe(2)
})
